
<style>
  .container {
    width: 800px;
    display: flex;
    justify-content: center;
    align-items: center;
    align-content: center;
    flex-direction: column;
  }

  .value {
    padding: 10px;
    background: #8257e6;
    color: #ffffff;
    border-radius: 6px;
    width: 15px;
    text-align: center;
    text-decoration: none;
    margin: 2px;
  }

  .level {
    display: flex;
    margin: 10px;
    justify-content: space-between;
    width: 350px;
  }

  .answers {
    width: 350px;
    display: flex;
    justify-content: space-between;
  }
</style>

<div class="container">
  <label>Olá <strong>{{name}}</strong>! Tudo bem?</label>

  <h3>{{title}}</h3>
  <br/>
  <strong>{{description}}</strong>

  <div class="level">
    <span>Pouco provável</span>
    <span>Muito provável</span>
  </div>

  <div class="answers">
    <a href="{{link}}/1?s={{survey_id}}&u={{user_id}}" class="value">1</a>
    <a href="{{link}}/2?s={{survey_id}}&u={{user_id}}" class="value">2</a>
    <a href="{{link}}/3?s={{survey_id}}&u={{user_id}}" class="value">3</a>
    <a href="{{link}}/4?s={{survey_id}}&u={{user_id}}" class="value">4</a>
    <a href="{{link}}/5?s={{survey_id}}&u={{user_id}}" class="value">5</a>
    <a href="{{link}}/6?s={{survey_id}}&u={{user_id}}" class="value">6</a>
    <a href="{{link}}/7?s={{survey_id}}&u={{user_id}}" class="value">7</a>
    <a href="{{link}}/8?s={{survey_id}}&u={{user_id}}" class="value">8</a>
    <a href="{{link}}/9?s={{survey_id}}&u={{user_id}}" class="value">9</a>
    <a href="{{link}}/10?s={{survey_id}}&u={{user_id}}" class="value">10</a>
  </div>

  <br />
  <br />

  <strong>Sua opinião é muito importante para nós</strong>
  <h3>Equipe | <strong>NLW</strong></h3>
</div>

